## 0.1.0

- Initial release: `CrashReportingInterface`/`CrashReportingService` with a PII
  guard, `FirebaseCrashReporter`, `LoggingCrashReporter`, `runGuardedApp`,
  `CrashReportingInterceptor` and `AppBlocObserver`.
