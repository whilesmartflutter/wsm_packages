name: wsm_core
description: WhileSmart mobile shared core — Failure model, ApiException hierarchy, error handlers, use-case base classes, and logger factory.
version: 0.1.0
homepage: https://github.com/whilesmartflutter/wsm_packages
repository: https://github.com/whilesmartflutter/wsm_packages
issue_tracker: https://github.com/whilesmartflutter/wsm_packages/issues
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  dio: ^5.8.0
  equatable: ^2.0.7
  fpdart: ^1.1.1
  logger: ^2.5.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
